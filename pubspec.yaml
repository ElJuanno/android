name: pru
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.2 # ← NUEVO: guarda el token
  url_launcher: ^6.1.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Dieta.png  # Asegúrate de que esta ruta sea correcta
    - assets/logo.png
    - assets/sobre.png
    - assets/plan_alimenticio.png
    - assets/registro_comidas.png